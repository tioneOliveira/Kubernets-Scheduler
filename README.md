# Kubernets-Scheduler
This repository contains a Python-based custom Kubernetes scheduler that replaces default scheduling with a Greedy Algorithm optimized for both real-time hardware constraints (CPU/Memory) and network latency.
